# NES 场景梳理与交互设计

## 设计目标

- ✅ **完全对齐 GitHub Copilot 截图**（箭头 + 蓝色高亮）
- ✅ **包含文本预览**（让用户看到具体修改内容）
- ✅ **新增和替换都需要预览**
- ✅ **2 次 Tab 交互**（跳转 + 预览 → 应用）

---

## 一、场景分类

根据 `docs/refactor/target.md` 文档，GitHub Copilot NES 支持以下场景：

### 1. 捕获和修正错误（Catching and Correcting Mistakes）

#### 1.1 拼写错误（Typo Fix）
**描述：** `cont x = 5` 或 `conts x = 5` → `const x = 5`

**场景类型：** ❌ **不属于 NES**
- 这是实时补全（FIM）或拼写修正（WORD_FIX）的职责
- 应该由 `FastCompletionProvider` 或 `WordFixProvider` 处理
- 优先级最高（WORD_FIX: priority 3）

**不纳入 NES 设计范围**

---

#### 1.2 逻辑错误修正（Logic Fix）

##### 场景 1.2.1：三元表达式错误
**触发条件：**
```typescript
// 用户编辑了函数，但逻辑有误
function findMax(a: number, b: number): number {
  return a > b ? b : a;  // ❌ 错误：应该返回 a，但返回了 b
}
```

**NES 预测：**
```typescript
// 建议修改第 9 行
return a > b ? a : b;  // ✅ 修正逻辑
```

**场景类型：** **REPLACE（替换）**
- 替换现有行的内容
- 修正逻辑错误

**视觉表现：**
- 第 9 行显示：**箭头 ⚡ + 蓝色高亮**
- 跳转后显示：**灰色文本预览**（`return a > b ? a : b;`）

**交互流程：**
```
1. 用户编辑代码 → NES 检测到逻辑错误
2. 显示：箭头 ⚡ + 蓝色高亮（第 9 行）
3. 用户按 Tab (第1次) → 跳转到第 9 行 + 显示灰色文本预览
4. 用户按 Tab (第2次) → 应用修改
```

---

##### 场景 1.2.2：逻辑运算符错误
**触发条件：**
```typescript
// 用户编辑了条件判断，但逻辑有误
function isValidStatus(value: string): boolean {
  if (value !== null || value !== undefined) {  // ❌ 错误：应该用 &&
    console.log("Valid");
    return true;
  }
  return false;
}
```

**NES 预测：**
```typescript
// 建议修改第 9 行
if (value !== null && value !== undefined) {  // ✅ 修正逻辑
```

**场景类型：** **REPLACE（替换）**

**视觉表现：**
- 第 9 行显示：**箭头 ⚡ + 蓝色高亮**
- 跳转后显示：**灰色文本预览**（`if (value !== null && value !== undefined) {`）

**交互流程：** 同场景 1.2.1

---

### 2. 意图变更（Changing Intent）

##### 场景 2.1：类定义扩展（添加属性）
**触发条件：**
```typescript
// 用户将 Point 改为 Point3D
class Point3D {
  x: number;
  y: number;
  // 光标在这里，缺少 z 属性
}
```

**NES 预测：**
```typescript
// 建议在第 12 行插入新内容
z: number;  // ✅ 添加 z 属性
```

**场景类型：** **INSERT（插入）**
- 在现有内容之后插入新行
- 添加新属性

**视觉表现：**
- 第 12 行显示：**箭头 💡 + 蓝色高亮**（或使用统一的 ⚡）
- 跳转后显示：**灰色文本预览**（`  z: number;`）

**交互流程：**
```
1. 用户编辑类名 Point → Point3D
2. NES 检测到意图变更，预测需要添加 z 属性
3. 显示：箭头 💡 + 蓝色高亮（第 12 行，y 属性之后）
4. 用户按 Tab (第1次) → 跳转到第 12 行 + 显示灰色文本预览
5. 用户按 Tab (第2次) → 插入新行
```

---

##### 场景 2.2：函数逻辑更新（修改计算）- 场景5第二个建议
**触发条件：**
```typescript
// 用户接受了场景 2.1 的建议，添加了 z 属性
class Point3D {
  x: number;
  y: number;
  z: number;  // ✅ 已添加
}

// 现在需要更新 distance 计算
getDistance() {
  return Math.sqrt(this.x ** 2 + this.y ** 2);  // ❌ 缺少 z
}
```

**NES 预测：**
```typescript
// 建议在第 11 行的 this.y ** 2 后面添加
return Math.sqrt(this.x ** 2 + this.y ** 2 + this.z ** 2);  // ✅ 添加 z**2
```

**场景类型：** **INLINE_INSERT（行内插入）**
- 在现有表达式中插入新的代码片段
- 不替换整行，只添加部分内容

**视觉表现：**
- 第 11 行显示：**箭头 ⚡ + 行内绿色片段**（`+ this.z ** 2`）
- 直接显示预览，不需要状态1

**交互流程：**
```
1. 用户接受场景 2.1 的建议（添加 z 属性）
2. NES 检测到需要更新计算逻辑
3. 直接显示：箭头 ⚡ + 行内绿色片段（在 this.y ** 2 后面）
4. 用户按 Tab → 应用修改
```

**关键实现（已在 NesEditor.vue 中验证）：**
```typescript
// 使用 before 装饰器在 ); 之前插入预览
ghostTextDecorations = editorRef.value.deltaDecorations([], [{
  range: new monaco.Range(targetLine, closingParenPosition, targetLine, closingParenPosition),
  options: {
    before: {
      content: ' + this.z ** 2',
      inlineClassName: 'nes-demo-inline-insert-preview',
      inlineClassNameAffectsLetterSpacing: true  // 🔑 关键配置1
    },
    showIfCollapsed: true  // 🔑 关键配置2
  }
}]);
```

**关键配置说明：**
- `inlineClassNameAffectsLetterSpacing: true` - 确保内联样式类影响字符间距
- `showIfCollapsed: true` - 确保装饰器在所有情况下都显示

**对应截图：** image-3.png（第二个建议）

---

### 3. 重构（Refactoring）

##### 场景 3.1：变量重命名
**触发条件：**
```typescript
// 用户重命名变量
const userName = "Alice";  // 原来是 name

// 其他地方还在使用旧名称
console.log("User:", name);  // ❌ 应该改为 userName
const greeting = `Hello, ${name}`;  // ❌ 应该改为 userName
```

**NES 预测：**
```typescript
// 建议修改第 15 行
console.log("User:", userName);  // ✅ 更新变量名

// 建议修改第 16 行
const greeting = `Hello, ${userName}`;  // ✅ 更新变量名
```

**场景类型：** **REPLACE（替换）**
- 替换变量名
- 可能有多个建议（队列）

**视觉表现：**
- 第 15 行显示：**箭头 ⚡ + 蓝色高亮**
- 跳转后显示：**灰色文本预览**（`console.log("User:", userName);`）

**交互流程：**
```
1. 用户重命名变量 name → userName
2. NES 检测到重命名，预测需要更新所有引用
3. 显示：箭头 ⚡ + 蓝色高亮（第 15 行）
4. 用户按 Tab (第1次) → 跳转到第 15 行 + 显示灰色文本预览
5. 用户按 Tab (第2次) → 应用修改
6. 自动跳转到下一个建议（第 16 行）
7. 重复步骤 4-5
```

---

##### 场景 3.2：命名模式更新
**触发条件：**
```typescript
// 用户修改了命名模式
const user_name = "Alice";  // 原来是 userName（驼峰）

// 其他变量还在使用驼峰命名
const userAge = 30;  // ❌ 应该改为 user_age
const userEmail = "alice@example.com";  // ❌ 应该改为 user_email
```

**NES 预测：**
```typescript
// 建议修改第 12 行
const user_age = 30;  // ✅ 统一命名风格

// 建议修改第 13 行
const user_email = "alice@example.com";  // ✅ 统一命名风格
```

**场景类型：** **REPLACE（替换）**

**视觉表现：** 同场景 3.1

**交互流程：** 同场景 3.1

---

##### 场景 3.3：代码风格匹配（Copy-Paste）
**触发条件：**
```typescript
// 用户从其他地方复制了代码
function calculateTotal(items) {  // ❌ 缺少类型注解
  return items.reduce((sum, item) => sum + item.price, 0);
}

// 当前文件的风格是带类型注解的
function calculateAverage(items: Item[]): number {
  return calculateTotal(items) / items.length;
}
```

**NES 预测：**
```typescript
// 建议修改第 8 行
function calculateTotal(items: Item[]): number {  // ✅ 添加类型注解
```

**场景类型：** **REPLACE（替换）**

**视觉表现：** 同场景 1.2.1

**交互流程：** 同场景 1.2.1

---

### 4. 删除（Delete）

##### 场景 4.1：删除无用代码
**触发条件：**
```typescript
// 用户删除了某个导入的使用
import { ref } from 'vue';
import { computed } from 'vue';  // ❌ 未使用，应该删除
import { watch } from 'vue';

const count = ref(0);
const doubled = watch(() => count.value * 2);
```

**NES 预测：**
```typescript
// 建议删除第 5 行
// （删除 import { computed } from 'vue';）
```

**场景类型：** **DELETE（删除）**
- 删除无用的代码行
- 无预览（直接删除）

**视觉表现：**
- 第 5 行显示：**箭头 ⚠️ + 红色高亮**
- 无预览（直接删除）

**交互流程：**
```
1. 用户删除了 computed 的使用
2. NES 检测到导入未使用
3. 显示：箭头 ⚠️ + 红色高亮（第 5 行）
4. 用户按 Tab (第1次) → 跳转到第 5 行
5. 用户按 Tab (第2次) → 删除该行
```

**对应 ChangeType：** DELETE

**已在 NesEditor.vue 中实现**

---

## 二、统一的交互设计

### 2.1 视觉表现（完全对齐截图 + 文本预览）

#### 状态 1：建议出现（未跳转）
```
┌─────────────────────────────────────────┐
│  8  function findMax(a, b) {            │
│  9 ⚡ return a > b ? b : a;             │  ← 箭头 + 蓝色高亮
│ 10  }                                   │
└─────────────────────────────────────────┘
```

**视觉元素：**
- **箭头图标 ⚡**（Glyph Margin）
- **蓝色高亮**（整行背景色）
- **Hover 提示**（鼠标悬停显示解释）

---

#### 状态 2：跳转后（显示预览）
```
┌─────────────────────────────────────────┐
│  8  function findMax(a, b) {            │
│  9 ⚡ return a > b ? b : a;             │  ← 原代码（保持可见）
│     return a > b ? a : b;               │  ← 灰色文本预览（inline ghost text）
│ 10  }                                   │
└─────────────────────────────────────────┘
```

**视觉元素：**
- **原代码保持可见**（不删除，不添加删除线）
- **灰色文本预览**（在原代码下方，使用 `after` 装饰器）
- **光标定位**（在原代码行）

---

#### 状态 3：应用后
```
┌─────────────────────────────────────────┐
│  8  function findMax(a, b) {            │
│  9  return a > b ? a : b;               │  ← 已应用修改
│ 10  }                                   │
└─────────────────────────────────────────┘
```

**视觉元素：**
- 清除所有装饰（箭头、高亮、灰色文本）
- 代码已更新

---

### 2.2 INSERT 模式的特殊处理

#### 状态 1：建议出现（未跳转）
```
┌─────────────────────────────────────────┐
│ 10  class Point3D {                     │
│ 11    x: number;                        │
│ 12 💡 y: number;                        │  ← 箭头 + 蓝色高亮（指向插入位置）
│ 13  }                                   │
└─────────────────────────────────────────┘
```

**说明：**
- 箭头指向 `y: number;` 这一行
- 表示将在这一行**之后**插入新内容

---

#### 状态 2：跳转后（显示预览）
```
┌─────────────────────────────────────────┐
│ 10  class Point3D {                     │
│ 11    x: number;                        │
│ 12 💡 y: number;                        │  ← 原代码（保持可见）
│       z: number;                        │  ← 灰色文本预览（将插入的内容）
│ 13  }                                   │
└─────────────────────────────────────────┘
```

**视觉元素：**
- **原代码保持可见**
- **灰色文本预览**（在原代码下方，表示将插入的新行）

---

#### 状态 3：应用后
```
┌─────────────────────────────────────────┐
│ 10  class Point3D {                     │
│ 11    x: number;                        │
│ 12    y: number;                        │
│ 13    z: number;                        │  ← 已插入新行
│ 14  }                                   │
└─────────────────────────────────────────┘
```

---

### 2.3 Tab 键交互流程（统一）

```
┌─────────────────────────────────────────┐
│         用户编辑代码                    │
│              ↓                          │
│      NES 检测到编辑模式                 │
│              ↓                          │
│      显示：箭头 + 蓝色高亮              │
│              ↓                          │
│   用户按 Tab (第1次)                    │
│              ↓                          │
│   跳转到建议行 + 显示灰色文本预览       │
│              ↓                          │
│   用户按 Tab (第2次)                    │
│              ↓                          │
│   应用修改（REPLACE 或 INSERT）         │
│              ↓                          │
│   如果有下一个建议，自动跳转            │
│   否则，清除所有装饰                    │
└─────────────────────────────────────────┘
```

---

## 三、技术实现方案

### 3.1 装饰器（Decorations）

#### Glyph Icon（箭头图标）
```typescript
const glyphDecoration = {
  range: new monaco.Range(targetLine, 1, targetLine, 1),
  options: {
    glyphMarginClassName: 'nes-glyph-icon',
    glyphMarginHoverMessage: {
      value: `**${changeType}**\n\n${explanation}`
    }
  }
};
```

#### 蓝色高亮（整行背景）
```typescript
const highlightDecoration = {
  range: new monaco.Range(targetLine, 1, targetLine, maxColumn),
  options: {
    className: 'nes-line-highlight',  // 蓝色背景
    isWholeLine: true
  }
};
```

#### 灰色文本预览（Inline Ghost Text）
```typescript
// REPLACE 模式：在原代码下方显示预览
const ghostTextDecoration = {
  range: new monaco.Range(targetLine, maxColumn, targetLine, maxColumn),
  options: {
    after: {
      content: `\n${suggestionText}`,  // 换行 + 建议文本
      inlineClassName: 'nes-ghost-text'  // 灰色样式
    }
  }
};

// INSERT 模式：在原代码下方显示预览
const ghostTextDecoration = {
  range: new monaco.Range(targetLine, maxColumn, targetLine, maxColumn),
  options: {
    after: {
      content: `\n${suggestionText}`,  // 换行 + 建议文本
      inlineClassName: 'nes-ghost-text-insert'  // 灰色样式（可能需要缩进）
    }
  }
};
```

---

### 3.2 CSS 样式

```css
/* 箭头图标 */
.nes-glyph-icon::before {
  content: '⚡';
  font-size: 16px;
  color: #007acc;
}

/* 蓝色高亮 */
.nes-line-highlight {
  background-color: rgba(0, 122, 204, 0.1);  /* 浅蓝色背景 */
  border-left: 3px solid #007acc;  /* 左侧蓝色边框 */
}

/* 灰色文本预览 */
.nes-ghost-text {
  color: #6c757d;
  opacity: 0.6;
  font-style: italic;
}

.nes-ghost-text-insert {
  color: #6c757d;
  opacity: 0.6;
  font-style: italic;
  /* 可能需要添加缩进 */
}
```

---

### 3.3 NESRenderer 实现

```typescript
// NESRenderer.ts
export class NESRenderer {
  /**
   * 显示建议（箭头 + 蓝色高亮）
   */
  public showSuggestion(prediction: Prediction): void {
    const { targetLine, changeType, explanation } = prediction;
    
    // 1. 显示 Glyph Icon
    this.decorationManager.renderGlyphIcon(targetLine, changeType, explanation);
    
    // 2. 显示蓝色高亮
    this.decorationManager.highlightLine(targetLine);
  }
  
  /**
   * 显示预览（灰色文本）
   */
  public showPreview(prediction: Prediction): void {
    const { targetLine, suggestionText, changeType } = prediction;
    
    if (changeType === 'REPLACE') {
      // 替换模式：在原代码下方显示预览
      this.decorationManager.showGhostText(targetLine, suggestionText);
    } else if (changeType === 'INSERT') {
      // 插入模式：在原代码下方显示预览（可能需要缩进）
      this.decorationManager.showGhostTextInsert(targetLine, suggestionText);
    }
  }
  
  /**
   * 应用建议
   */
  public applySuggestion(prediction: Prediction): void {
    const { targetLine, suggestionText, changeType } = prediction;
    const model = this.editor.getModel();
    if (!model) return;
    
    if (changeType === 'REPLACE') {
      // 替换整行
      const edit = {
        range: new monaco.Range(targetLine, 1, targetLine, model.getLineMaxColumn(targetLine)),
        text: suggestionText,
        forceMoveMarkers: true
      };
      this.editor.executeEdits('nes-replace', [edit]);
    } else if (changeType === 'INSERT') {
      // 在当前行之后插入新行
      const edit = {
        range: new monaco.Range(targetLine, model.getLineMaxColumn(targetLine), targetLine, model.getLineMaxColumn(targetLine)),
        text: `\n${suggestionText}`,
        forceMoveMarkers: true
      };
      this.editor.executeEdits('nes-insert', [edit]);
    }
    
    // 清除装饰
    this.clear();
  }
}
```

---

### 3.4 NESController 实现

```typescript
// NESController.ts
public applySuggestion(): void {
  if (!this.hasActiveSuggestion()) {
    return;
  }

  const prediction = this.suggestionQueue.current();
  if (!prediction) return;

  // 场景 1：用户不在建议行 → 跳转 + 显示预览
  if (!this.isUserOnSuggestionLine) {
    console.log('[NESController] 🧭 Navigating to suggestion line');
    
    // 跳转到建议行
    this.jumpToSuggestionWithSmartCursor(prediction);
    this.isUserOnSuggestionLine = true;
    
    // 显示灰色文本预览
    this.renderer.showPreview(prediction);
    
    // 更新 HintBar
    this.updateHintBar(prediction);
    return;
  }

  // 场景 2：用户在建议行 → 应用建议
  console.log('[NESController] ✅ Accepting suggestion (applying code)');
  this.acceptSuggestion();
}
```

---

## 四、场景总结表

| 场景 | 类型 | 触发条件 | 视觉表现 | 交互流程 | 实现状态 |
|------|------|----------|----------|----------|----------|
| 逻辑错误修正 | REPLACE | 检测到逻辑错误 | 箭头 ⚡ + 整行红色 + 整行绿色预览 | Tab 跳转 + 预览 → Tab 应用 | ✅ 场景1 |
| 类定义扩展 | INSERT | 类名变更（Point → Point3D） | 箭头 💡 + 整行蓝色 + 整行绿色预览 | Tab 跳转 + 预览 → Tab 应用 | ✅ 场景2 |
| 函数逻辑更新 | INLINE_INSERT | 类属性变更后 | 箭头 ⚡ + 行内绿色片段 | 直接显示预览 → Tab 应用 | ✅ 场景5第2个 |
| 变量重命名 | REPLACE | 检测到变量重命名 | 箭头 ⚡ + 只高亮单词 + 行内箭头 | Tab 跳转 + 预览 → Tab 应用（多次） | ✅ 场景3 |
| 命名模式更新 | REPLACE | 检测到命名风格变更 | 箭头 ⚡ + 只高亮单词 + 行内箭头 | Tab 跳转 + 预览 → Tab 应用（多次） | ✅ 场景3 |
| 代码风格匹配 | REPLACE | 检测到粘贴代码 | 箭头 ⚡ + 整行红色 + 整行绿色预览 | Tab 跳转 + 预览 → Tab 应用 | ✅ 场景1 |
| 删除无用代码 | DELETE | 检测到未使用的导入/代码 | 箭头 ⚠️ + 整行红色 | Tab 跳转 → Tab 删除 | ✅ 场景4 |

---

## 五、与当前实现的差异

### 当前实现（NesEditor.vue 已完成）
- ✅ 使用 ViewZone 显示整行预览（场景1、2）
- ✅ 使用行内箭头 + ViewZone 显示单词预览（场景3）
- ✅ 使用 before 装饰器显示行内片段（场景5第2个）
- ✅ 只需 2 次 Tab（跳转 + 预览 → 应用）
- ✅ 预览清晰可见，不干扰原代码
- ✅ 箭头 + 高亮（完全对齐截图）
- ✅ 实现了 5 种场景的 UI 演示

### 关键技术突破
1. **ViewZone 精确对齐** - 使用 `leadingSpaces` 保持缩进一致
2. **行内箭头对齐** - 使用 `wordStartIndex` 精确定位
3. **before/after 装饰器显示** - 添加 `inlineClassNameAffectsLetterSpacing` 和 `showIfCollapsed`
4. **连续建议实现** - 第1个用 ViewZone，第2个用 before 装饰器

### 待迁移到生产代码
- [ ] 将 NesEditor.vue 的 UI 实现迁移到 NESRenderer.ts
- [ ] 实现后端 Prompt 返回 changeType
- [ ] 实现触发逻辑分离
- [ ] 端到端测试

---

## 六、实施优先级

### Phase 1：核心功能（已完成 ✅）
- ✅ 实现灰色文本预览（ViewZone + before 装饰器）
- ✅ 简化 Tab 键逻辑（3次 → 2次）
- ✅ 实现 REPLACE、INSERT、DELETE、INLINE_INSERT 四种模式
- ✅ 测试基本交互流程（NesEditor.vue 演示）

### Phase 2：视觉优化（已完成 ✅）
- ✅ 优化高亮样式（红色/蓝色背景）
- ✅ 优化箭头图标样式（⚡/💡/⚠️）
- ✅ 优化灰色文本样式（颜色、透明度、斜体）
- ✅ 测试视觉效果（5个场景全部验证）

### Phase 3：场景支持（待实现）
- [ ] 实现逻辑错误检测（后端 Prompt）
- [ ] 实现意图变更检测（后端 Prompt）
- [ ] 实现变量重命名检测（后端 Prompt）
- [ ] 测试所有场景

### Phase 4：代码迁移（待实现）
- [ ] 将 NesEditor.vue 的 UI 实现迁移到 NESRenderer.ts
- [ ] 更新 NESController.ts 支持新的交互流程
- [ ] 更新后端 Prompt 返回 changeType
- [ ] 端到端测试

### Phase 5：性能优化（可选）
- [ ] 优化触发频率（减少无效预测）
- [ ] 添加缓存机制
- [ ] 优化响应时间

---

## 七、验收标准

### 功能验收
- ✅ 支持 REPLACE、INSERT、DELETE、INLINE_INSERT 四种模式
- ✅ Tab 键只需 2 次（跳转 + 预览 → 应用）
- ✅ 显示灰色文本预览（ViewZone + before 装饰器）
- ✅ 箭头 + 高亮（对齐截图）
- ✅ 5 种场景全部实现（NesEditor.vue 演示）

### 视觉验收
- ✅ 高亮样式与截图一致（红色/蓝色背景）
- ✅ 灰色文本清晰可见但不干扰阅读
- ✅ 箭头图标位置正确（⚡/💡/⚠️）
- ✅ 行内片段正确显示（场景5第2个）

### 交互验收
- ✅ 用户清楚知道下一步操作（视觉反馈明确）
- ✅ 预览内容准确（与最终应用的内容一致）
- ✅ 多个建议时自动跳转到下一个（场景5演示）

### 场景验收
- ✅ 逻辑错误修正场景正常工作（场景1）
- ✅ 意图变更场景正常工作（场景2、场景5）
- ✅ 变量重命名场景正常工作（场景3）
- ✅ 删除无用代码场景正常工作（场景4）
- ✅ 连续建议场景正常工作（场景5）

### 技术验收
- ✅ ViewZone 精确对齐（缩进保持一致）
- ✅ 行内箭头精确对齐（使用 wordStartIndex）
- ✅ before/after 装饰器正确显示（关键配置已添加）
- ✅ 代码可直接迁移到 NESRenderer.ts
